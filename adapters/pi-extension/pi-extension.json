{
  "name": "ant-colony",
  "version": "0.1.0",
  "description": "Run the Ant code-cleanup colony from Pi. A thin shell that execs `ant <verb> --json` and parses the event stream; no engine logic (TECHSPEC §3).",
  "entry": "src/extension.ts",
  "commands": [
    {
      "name": "ant",
      "summary": "Run an Ant verb (scout/fix/review/apply) and report findings, verified fixes, and skips.",
      "argumentHint": "[scout|fix|review|apply] [path] [extra ant flags]",
      "run": "node src/run.ts"
    }
  ],
  "permissions": {
    "subprocess": ["ant"]
  }
}
