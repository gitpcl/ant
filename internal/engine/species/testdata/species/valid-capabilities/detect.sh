#!/bin/sh
# Stub command detector for the capabilities fixture: it only needs to exist so
# the loader's "referenced [detector].script must exist" check passes. No real
# detection runs in the loader-level capability tests.
exit 0
