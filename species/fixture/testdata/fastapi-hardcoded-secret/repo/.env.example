# Example environment for the fastapi-hardcoded-secret fixture. Documents the
# variables the application reads at runtime; it carries NAMES ONLY, never real
# values. The fastapi-hardcoded-secret fix appends SECRET_KEY= here when it
# relocates the credential out of source.
APP_NAME=fastapi-hardcoded-secret-fixture
API_KEY=
