# Example environment for the hardcoded-secret fixture. Documents the variables
# the application reads at runtime; it carries NAMES ONLY, never real values.
# The hardcoded-secret fix appends AWS_ACCESS_KEY_ID= here when it relocates the
# credential out of source.
APP_NAME=hardcodedsecret-fixture
