// total.js holds exactly ONE js-eqeqeq finding in plain JavaScript (the loose
// `!=` below) — the .js coverage the Sprint 025 wave adds via the
// `language: javascript` detector doc.
export function differs(a, b) {
	return a != b;
}
