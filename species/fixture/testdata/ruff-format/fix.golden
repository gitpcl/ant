--- a/app/service.py
+++ b/app/service.py
@@ -1,3 +1,3 @@
-# ant:ruff-format the function below has trailing-whitespace drift the formatter fixes
-def handle(name):
-    return name.strip()   
+# ant:ruff-format the function below has trailing-whitespace drift the formatter fixes
+def handle(name):
+    return name.strip()
