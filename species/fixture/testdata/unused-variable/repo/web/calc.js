// web/calc.js — Sprint 025 js-multilang-backfill .js coverage. The marked const
// below is never referenced; unused-variable now removes it in plain JavaScript.
export function area(w, h) {
	//ant:unused-variable leftover from a refactor
	const perimeter = 2 * (w + h);
	return w * h;
}
